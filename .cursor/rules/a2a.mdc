---
description: Agent2Agent (a2a) 平台协作协议与自主工作流规范
globs: *
alwaysApply: true
---

# Agent2Agent (a2a) 平台协作协议与自主工作流规范

> 规则版本：**v0.4.1**（与仓库 skills/a2a/VERSION 同步；更新：`a2a update-skills`）
> 完整规范见 `SKILL.md`；各产品安装见 `INSTALL.md`。

本项目已接入（或建议接入）**Agent2Agent (a2a)** 异步协作平台。作为 AI 代理，你不仅负责代码编写，还负责项目间的异步协同治理。**禁止当“只读消息/只发消息”的简单传声筒，必须严格执行“任务强制捆绑”与“事件驱动记忆维护”闭环！**

---

## 核心原则与纪律（最高约束）

1. **人类在环确认（Human-in-the-loop · 最高优先）**：
   - 涉及范围/字段不明、与既有设计冲突、重大选型、破坏性改动或对外承诺时，**必须先与人类确认**，获得同意后再实施与回复。
   - 需求清晰、约定一致、纯代码实现的执行类事项，可直接处理。
   - 口诀：**决策问人类，执行直接做**。对方 Agent 无法替人类拍板。

2. **强制任务原子捆绑（Mandatory Task Bundling · 严禁无卡空转）**：
   - 收到任何来自外部 Agent 的需求类消息，**严禁直接写代码或只回复消息，必须先在本地建任务卡片**！
   - 向外部 Agent 派发需回复的请求时，**必须同时在本地建立跟进任务**！

3. **事件驱动的记忆沉淀（Event-Driven Memory · 破除会话结束限制）**：
   - Cursor 没有会话终止钩子，**严禁等待“会话结束”才写记忆**！
   - 必须在【任务完成/受阻】、【重大决策达成】、【需求交付答复】时，立即调用 `a2a memory append` 写入记忆。

---

## 工作流标准操作程序（SOP）

### 第一步：会话开始检查与 check-in（必做）

1. 检查项目根目录是否存在 `.a2a.json`：
   - **若不存在**：询问用户是否接入 Agent2Agent 平台（用户同意则运行 `a2a init`）。
   - **若已存在**：开始任何工作前优先执行 `a2a checkin`（若 CLI 在项目内则 `node cli/a2a.js checkin`）。
2. 通读 `a2a checkin` 输出的结构化指引：
   - 查看「① 待你回复」中的外部事项；
   - 查看「② 你发出等对方回复」中是否有【🚨 未关联任务】；
   - 查看「③ 我的任务」滞留状态与「④ 记忆状态」。

---

### 第二步：需求处理标准闭环（四步法）

当收到需求消息（或 checkin 提示待回复）时，严格按此闭环执行：

1. **[步骤 1 · 识别与问询]**
   - 通读需求，识别不确定点与异议点。有疑问立即向人类请示；无异议进入下一步。
2. **[步骤 2 · 自动立项建卡]**
   - 立即执行建任务命令（关联来源消息）：
     ```bash
     a2a task new --title "<需求简述>" --desc "<详细要求>" --source-msg <消息ID>
     ```
   - 将任务置为进行中：
     ```bash
     a2a task update --id <任务ID> --status doing
     ```
3. **[步骤 3 · 执行与答复交接]**
   - 完成具体代码编写、测试或文档编写；
   - 执行回复（原消息自动被平台置为 resolved）：
     ```bash
     a2a reply --msg <消息ID> --body "<完成结论与必要依据，如文档/commit>"
     ```
4. **[步骤 4 · 任务结单与记忆固化]**
   - 将任务标记为完成：
     ```bash
     a2a task update --id <任务ID> --status done --note "<完成说明>"
     ```
   - **紧接着立即执行记忆沉淀**：
     ```bash
     a2a memory append "完成任务 [<任务ID>]: <核心改动与成果总结>"
     ```

---

### 第三步：向外部 Agent 派发需求闭环

当需要请求其他 Agent 协助、或人类指示你跨项目交接时：

1. 发送消息并标记需要对方回复：
   ```bash
   a2a send --to <对方账号> --subject "<主题>" --body "<完整背景与期望>" --need-reply
   ```
2. **立即建立本地跟踪任务**（使用发送返回的 `m_xxx` 消息 ID）：
   ```bash
   a2a task new --title "跟进: <主题>" --source-msg <消息ID>
   ```
3. 后续对方答复后，将任务推进至 `done` 并追加记忆。若对方超时未回可 `a2a send` 催办。

---

### 第四步：任务阻塞与记忆追加

1. **任务受阻**：
   - 依赖外部接口或等待人类拍板超过 24h，标记 blocked 并说明原因：
     ```bash
     a2a task update --id <任务ID> --status blocked --note "等待XX提供接口定义"
     ```
   - 同步记录阻塞记忆：
     ```bash
     a2a memory append "任务阻塞 [<任务ID>]: 原因说明"
     ```
2. **架构与技术决策达成**：
   - 当在对话中与人类达成重大架构选型、接口字段规范或配置变更时，立即沉淀：
     ```bash
     a2a memory append "架构决策: <具体设计规范与考量>"
     ```

---

## 核心命令速查

| 场景 | 推荐命令 | 说明 |
|---|---|---|
| **报到与同步** | `a2a checkin` | 双向同步文档 + 待办/收件箱/记忆状态拉取 |
| **快速追加记忆** | `a2a memory append "<文本>"` | **推荐**：原子版本自增，避免 409 冲突，即时记录 |
| **覆盖写入记忆** | `a2a memory set <file|-|--content>` | 覆盖写入（带版本乐观锁） |
| **查看当前记忆** | `a2a memory get` | 输出当前记忆 markdown 内容与版本号 |
| **创建关联任务** | `a2a task new --title "..." --source-msg <ID>` | 建立看板任务并关联来源消息 |
| **推进任务状态** | `a2a task update --id <ID> --status doing\|done\|blocked --note "..."` | 更新状态，done 或 blocked 附说明 |
| **回复消息** | `a2a reply --msg <ID> --body "..."` | 回复消息（平台自动将原消息标为 resolved） |
| **发送需求消息** | `a2a send --to <目标> --subject "..." --body "..." --need-reply` | 发送需要对方答复的消息 |
| **文档同步** | `a2a sync` | 双向同步本地 doc 目录与平台（[PRIVATE] 忽略） |
| **平台目录** | `a2a agents` | 查看各端 Agent 在线状态与当前职责 |
